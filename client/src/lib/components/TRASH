	class={`modal-overlay fixed top-0 right-0 w-[300px] h-full flex pointer-events-none ${$showContentEditor ? 'block' : 'hidden'}`}

	// If your edge has specific path options, include them as well:
	// export let pathOptions?: BezierPathOptions | SmoothStepPathOptions | StepPathOptions;

	// Props for EdgeLayouted, if needed:
	export let animated: boolean;
	export let selected: boolean;
	// Note: Add any additional props specific to your edge's logic.
	// type $$Props = DirectionalProps;
	// data?: any;
	// type: string;
	// markerStart?: string;
	// markerEnd?: string;
	// sourceHandleId?: string | null;
	// targetHandleId?: string | null;

	// export let id: string;
	// export let animated: string;
	// export let selected: string;
	// export let label: string;
	// export let interactionWidth: number;
	// export let labelStyle: string;
	// export let type: any;
	// export let target: string;
	// export let source: string;
	// export let sourceX: number; // Default values for numbers
	// export let sourceY: number;
	// export let sourcePosition: Position;
	// export let targetX: number = 0;
	// export let targetY: number = 0;
	// export let targetPosition: Position;
	// export let markerEnd: string = ''; // Default to empty if potentially undefined
	// export let markerStart: string = ''; // Default to empty if potentially undefined
	// export let data: any = null; // Default to null for 'any' type, if needed
	// export let sourceHandleId: string | null = null;
	// export let targetHandleId: string | null = null;
	// export let style: string = ''; // Ensure it's always a string

	// export let source: $$Props['source'];
	// export let sourceX: $$Props['sourceX'];
	// export let sourceY: $$Props['sourceY'];
	// export let sourcePosition: $$Props['sourcePosition'];
	// export let targetX: $$Props['targetX'];
	// export let targetY: $$Props['targetY'];
	// export let targetPosition: $$Props['targetPosition'];
	// export let style: $$Props['style'];
	// export let markerEnd: $$Props['markerEnd'] = undefined;